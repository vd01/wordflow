{
  "name": "wordflow-miniprogram",
  "version": "0.1.0",
  "private": true,
  "description": "WeChat Mini Program for WordFlow — FSRS-based spaced repetition using SyncServer words",
  "scripts": {
    "test": "jest",
    "test:unit": "jest --testPathIgnorePatterns=e2e",
    "test:e2e": "jest __tests__/e2e.test.js --testTimeout=60000",
    "ci:preview": "node ci/upload.js --env=preview",
    "ci:upload": "node ci/upload.js --env=upload"
  },
  "devDependencies": {
    "jest": "^30.4.2",
    "miniprogram-automator": "^0.12.1"
  },
  "dependencies": {
    "ts-fsrs": "^5.4.1"
  }
}
